jQuery plugins and Drupal behaviors for E-center project.

Table chart plugin:

The table chart jQuery plugin depends on jqPlot.

It can be invoked like this:

$('.graph-me').tablechart({
  height: 150,
  width: 500,
  plotOptions: {
    legend: {show:true},
    series: [{color: '#555555'}],
    seriesDefaults: {
      lineWidth:2, 
      //color: '#4a7ebb',
      shadow: false,
      markerOptions:{
        size: 7,
        shadow: false
      }
    },
    grid: {
      gridLineColor: '#ffffff',
      background: '#d9d9d9',
      shadow: false,
      borderWidth: 1
    },
    cursor: {
      zoom: true,
      showTooltip: false
    },
    axes: {
      xaxis:{
        renderer:$.jqplot.DateAxisRenderer,
        tickOptions:{
          formatString:'%H:%M:%S',
        },
      },
    }
  }
});
