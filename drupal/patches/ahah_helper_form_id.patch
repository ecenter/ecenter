diff --git a/ahah_helper.module b/ahah_helper.module
index 4a5855d..076296b 100644
--- a/ahah_helper.module
+++ b/ahah_helper.module
@@ -124,12 +124,12 @@ function ahah_helper_generic_submit($form, &$form_state) {
  */
 function ahah_helper_render($form_item_to_render = FALSE) {
   $form_state = array('storage' => NULL, 'submitted' => FALSE);
+  $form_id = $_POST['form_id'];
   $form_build_id = $_POST['form_build_id'];
 
   // Get the form from the cache.
   $form = form_get_cache($form_build_id, $form_state);
   $args = $form['#parameters'];
-  $form_id = array_shift($args);
   // We will run some of the submit handlers so we need to disable redirecting.
   $form['#redirect'] = FALSE;
   // We need to process the form, prepare for that by setting a few internals
